<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <!-- TICKET SERVICE | Author: shaparenko | Branch: ticket_service-nosql -->

    <changeSet id="401-drop-payment-table" author="shaparenko">

        <preConditions onFail="HALT">
            <tableExists tableName="payment"/>
        </preConditions>

        <dropAllForeignKeyConstraints baseTableName="payment"/>
        <dropTable tableName="payment"/>

        <sql>
            UPDATE tables
            SET description = '[REMOVED — moved to MongoDB]',
                last_update_at = NOW()
            WHERE name = 'payment';
        </sql>

        <rollback>
            <createTable tableName="payment">
                <column name="id" type="BIGSERIAL">
                    <constraints primaryKey="true" nullable="false"/>
                </column>
                <column name="ticket_id" type="BIGINT">
                    <constraints nullable="false" unique="true"/>
                </column>
                <column name="amount" type="DECIMAL(8,2)"><constraints nullable="false"/></column>
                <column name="method" type="VARCHAR(30)" defaultValue="CARD"/>
                <column name="status" type="VARCHAR(20)" defaultValue="PENDING"/>
                <column name="paid_at" type="TIMESTAMP"/>
                <column name="created_at" type="TIMESTAMP" defaultValueComputed="NOW()"/>
            </createTable>
            <addForeignKeyConstraint
                    baseTableName="payment" baseColumnNames="ticket_id"
                    referencedTableName="ticket" referencedColumnNames="id"
                    constraintName="fk_payment_ticket" onDelete="CASCADE"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
