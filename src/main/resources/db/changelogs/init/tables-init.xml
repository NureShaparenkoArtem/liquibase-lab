<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <changeSet id="000-create-tables-registry" author="teamlead">
        <createTable tableName="tables">
            <column name="id" type="SERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="db_" type="VARCHAR(128)">
                <constraints nullable="false"/>
            </column>
            <column name="name" type="VARCHAR(128)">
                <constraints nullable="false"/>
            </column>
            <column name="type" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
            <column name="author" type="VARCHAR(128)"/>
            <column name="version" type="INT" defaultValue="1"/>
            <column name="service" type="VARCHAR(128)"/>
            <column name="description" type="VARCHAR(500)"/>
            <column name="column_count" type="INT"/>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="NOW()"/>
            <column name="last_update_at" type="TIMESTAMP"/>
        </createTable>

        <sql>
            INSERT INTO tables (db_, name, type, author, version, service, description, column_count, created_at, last_update_at)
            VALUES ('liquibase_lab', 'tables', 'SYSTEM', 'template', 1, NULL,
                    'Metadata registry of all tables in the database',
                    (SELECT COUNT(*) FROM information_schema.columns
                     WHERE table_schema = 'public' AND table_name = 'tables'),
                    NOW(), NULL);
        </sql>

        <rollback>
            <sql>DELETE FROM tables WHERE name = 'tables';</sql>
            <dropTable tableName="tables"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
